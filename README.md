# socials-backend (deprecated)

This Express server is **no longer used**.

The Socials app now talks to MongoDB from Next.js API routes in [`karanjoshi1206/socials`](https://github.com/karanjoshi1206/socials) under `/api/serverApi`. Deploy that repo on Vercel and set `SOCIALS_MONGO_DB_URL` there.

Keep this repository only as a historical reference, or archive it after the Next.js deployment is confirmed working.
